# Copy to `.env` to override defaults used by docker-compose.yml.
# All values below are the defaults, so `.env` is optional.

# Postgres
POSTGRES_DB=library
POSTGRES_USER=library
POSTGRES_PASSWORD=library

# Host ports
DB_PORT=5432
BACKEND_PORT=9999
FRONTEND_PORT=5173
